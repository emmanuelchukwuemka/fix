// JavaScript for dashboard page interactivity (placeholder for future features)

document.addEventListener("DOMContentLoaded", function () {
  // Example: Add event listeners for task items (future enhancement)
  const tasks = document.querySelectorAll("main section ul li");
  tasks.forEach((task) => {
    task.addEventListener("click", () => {
      alert("Task details and actions coming soon!");
    });
  });
});
